import styled from "styled-components";

export const CADiv = styled.div`
    display: flex;
    width: 66.6%;
    margin: 0 auto 160px auto;
`;

export const CABtn = styled.button`
    width: 100%;
    border-radius: 4px;
    border: 2px solid var(--Primary);
    height: 80px;
    background-color: white;
    color: var(--Primary);
    font: var(--PC_ButtonText);
    cursor: pointer;
`;